// Shared bounded event vocabulary for the JakeAI Universe.
export const EVENT_TYPES=new Set(['DISCOVERY_FOUND','PRODUCT_BUILT','QA_FAILURE','ROOT_CAUSE_REPAIRED','AGENT_MATCH_COMPLETED','DISTRICT_UNLOCKED','CAMPAIGN_LAUNCHED','MARKET_RESPONSE','COUNCIL_DECISION','ANOMALY_SURVIVED','SECRET_DISCOVERED']);
export function universeEvent(type,{source='factory',label='',deltas={},refs=[]}={}){if(!EVENT_TYPES.has(type))throw new Error('UNKNOWN_UNIVERSE_EVENT');return Object.freeze({id:`${type}:${Date.now()}:${Math.random().toString(36).slice(2,7)}`,type,source,label,deltas:sanitizeDeltas(deltas),refs:[...refs].slice(0,8),at:Date.now()})}
function sanitizeDeltas(d){const allowed=['xp','learning','products','reputation','reliability','discovery','velocity','efficiency'];return Object.fromEntries(Object.entries(d).filter(([k,v])=>allowed.includes(k)&&Number.isFinite(v)))}
export function applyUniverseEvent(save,event){if(!EVENT_TYPES.has(event?.type))return{accepted:false,error:'INVALID_EVENT',save};save.universeEvents=save.universeEvents||[];save.universeEvents.push(event);save.universeEvents=save.universeEvents.slice(-100);return{accepted:true,save,event}}
export function unresolvedHooks(save){const events=save.universeEvents||[],last=events.at(-1),hooks=[];if(last?.type==='ROOT_CAUSE_REPAIRED')hooks.push({world:'Council Chamber',reason:'The Council wants the repair evidence.'});if(last?.type==='AGENT_MATCH_COMPLETED')hooks.push({world:'Agent Arena',reason:'A rival agent has adapted to the last match.'});if(last?.type==='DISCOVERY_FOUND')hooks.push({world:'The Factory',reason:'A discovered opportunity is ready to become a product.'});return hooks}
