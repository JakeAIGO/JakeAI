name: Apply Baseline Repairs — RETIRED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired remediation mechanism
        run: |
          echo "The write-capable Master Baseline repair workflow is retired."
          echo "No repository mutation, deployment, publication, checkout activation, or production action is performed here."
          echo "Future changes must use normal reviewable branch commits and the read-only Master Baseline Gate."
