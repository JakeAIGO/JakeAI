const hero=document.querySelector('.scene>img');if(hero&&window.JAI_IMG){hero.src='data:image/webp;base64,'+window.JAI_IMG;}
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const s=a.getAttribute('href'),el=document.querySelector(s);if(!el)return;e.preventDefault();if(a.classList.contains('room')){const t=document.querySelector('.transition');t.querySelector('span').textContent='ENTERING '+a.querySelector('b').textContent.replace(/\s+/g,' ');t.classList.add('on');setTimeout(()=>{t.classList.remove('on');el.scrollIntoView({behavior:'smooth',block:'start'})},360)}else el.scrollIntoView({behavior:'smooth'})}));document.querySelectorAll('[data-enter]').forEach(b=>b.addEventListener('click',()=>{const name=b.dataset.enter;alert(name+' room shell is staged. Final production route/content is release-gated until approved.')}));