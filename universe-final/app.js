document.querySelectorAll('.hot').forEach(link=>{link.addEventListener('click',e=>{e.preventDefault();const t=document.querySelector('.portal-transition');t.querySelector('b').textContent='ENTERING '+link.getAttribute('aria-label').toUpperCase();t.classList.add('on');setTimeout(()=>location.href=link.href,260)});});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth',block:'start'})}}));